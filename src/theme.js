import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#888',
      light: 'skyBlue',
    },
    secondary: {
      main: '#15b678',
    },
    otherColor: {
      main: 'red',
    },
  },
});
